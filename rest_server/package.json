{
  "name": "movie_rest_server",
  "version": "1.0.0",
  "description": "A simple express rest server to be used as a docker service.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon src/index.js --exec babel-node",
    "build": "babel src -d dist --copy-files",
    "serve": "node dist/index.js"
  },
  "author": "Yannick Bals",
  "license": "ISC",
  "dependencies": {
    "es6-promise": "^4.2.4",
    "express": "^4.16.2",
    "isomorphic-fetch": "^2.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "nodemon": "^1.17.1"
  }
}
